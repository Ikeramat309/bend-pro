# Conduit Data

Future conduit type, trade size, material, and metadata modules should live here.

Current working conduit data remains in `src/data/conduitTypes.ts` for stability.

Do not move or rewrite the active conduit data system until a task specifically asks for that migration.
