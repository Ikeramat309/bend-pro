/**
 * FILE: src/navigation/types.ts
 *
 * PURPOSE: TypeScript names for each screen in the stack (for typed navigation later).
 * BEGINNER NOTE: `undefined` means the route takes no parameters (?id= not used yet).
 */

// TYPES
export type RootStackParamList = {
  Home: undefined;
  CalculatorWorkbench: undefined;
  Settings: undefined;
  BenderDatabase: undefined;
};

// EXPORTS — above
